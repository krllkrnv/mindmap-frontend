services:
  mindmap-vkr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mindmap-vkr
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
    networks:
      - mindmap-network

networks:
  mindmap-network:
    driver: bridge
